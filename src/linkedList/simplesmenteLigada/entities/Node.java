package linkedList.simplesmenteLigada.entities;

public class Node {
    // atributos criados, estão como default
    int data;
    Node next;

    // construtor com argumentos
    public Node(int data) {
        this.data = data;
        this.next = null;
    }
}
